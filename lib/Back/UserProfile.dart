class UserProfile {
  String firstName;
  String lastName;
  // Ajoutez d'autres champs de profil ici selon votre structure de base de données.

  UserProfile({required this.firstName, required this.lastName});
}